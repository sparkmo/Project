services:
  intranet:
    build: .
    ports:
      - "8001:80"
    env_file:
      - .env
    restart: unless-stopped

# --- DB 컨테이너 없음 ---
# intranet_db / ott(회원 DB)는 이제 로컬 컨테이너가 아니라 별도 DB 서버
# (Project-DBServer, docker compose 별도 실행)에 접속합니다.
# .env 의 DB_HOST / MEMBER_DB_HOST 를 그 서버의 주소로 설정하세요.
