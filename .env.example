# 이 파일을 복사해서 .env 로 만들고 실제 값을 채우세요.
# .env 자체는 절대 커밋하지 마세요 (.gitignore에 포함되어 있음).
#
# 참고: 여기서 env로 값을 관리해도, 웹쉘(RCE)로 뚫리면 getenv()를 그대로
# 호출해서 값을 조회할 수 있으므로 이 시나리오에서 실질적 방어가 되지
# 않습니다 (VULN-HIGH-4).

INTRANET_API_TOKEN=여기에_실제_토큰_입력
